# Azure AD app registration used for Microsoft Graph (SharePoint) access.
# Requires an app-only (client credentials) grant with Sites.Read.All (or similar).
TENANT_ID=
CLIENT_ID=
CLIENT_SECRET=

# Separate public client app registration used only for interactive
# delegated login testing (test_login.py).
LOGIN_CLIENT_ID=
